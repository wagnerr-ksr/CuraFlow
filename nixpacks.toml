# Nixpacks Configuration for Railway
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd server && npm install --production"]

[phases.build]
cmds = []

[start]
cmd = "cd server && npm start"
